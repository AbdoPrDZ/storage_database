name: storage_database
description: A quick and simple offline StorageDatabase package for flutter
version: 2.3.0
homepage: https://github.com/AbdoPrDZ/storage_database

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  laravel_echo_null: ^0.3.0
  path: ^1.9.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.4
  pusher_client_socket: ^0.0.8
  socket_io_client: ^3.1.4
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: null
