name: storage_database
description: A quick and simple offline StorageDatabase package for flutter
version: 1.0.3
homepage: https://github.com/AbdoPrDZ/storage_database.git

environment:
  sdk: ">=3.2.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  laravel_echo_null: ^0.0.5+8
  path: ^1.8.3
  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  shimmer: ^3.0.0
  pusher_client_fixed: ^0.0.2+3
  socket_io_client: ^2.0.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: null
